type: object
properties:
  status:
    type: string
    enum:
      - queued
      - standby
      - initializing
      - recording
      - uploading
      - complete
      - error
    description: The status of this job.
