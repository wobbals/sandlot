type: object
description: >-
  An Extended Connection Created Event. TODO- This may split into multiple
  event types if the connection properties are reported separately (and
  optionally) from the guaranteed `connectionCreated` log line.
allOf:
  - $ref: '#/definitions/EEConnection'
properties:
  guid:
    type: string
    description: >-
      A persistent identifier for this endpoint. Usually consistant across
      multiple browser page loads, etc.
  tokboxServer:
    type: string
    description: The host name of the server that hosted this connection.
  permissions:
    type: array
    items:
      type: string
      enum:
        - publish
        - subscribe
        - signal
        - moderation
  userAgent:
    type: string
