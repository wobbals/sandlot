get:
  tags:
    - Session Objects
  description: Describe a Subscriber
  parameters:
    - $ref: '#/parameters/resourceId'
  responses:
    200:
      description: OK
      schema:
        $ref: '#/definitions/Subscriber'
