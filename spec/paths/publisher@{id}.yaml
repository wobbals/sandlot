get:
  tags:
    - Session Objects
  description: Describe a Publisher
  parameters:
    - $ref: '#/parameters/resourceId'
  responses:
    200:
      description: OK
      schema:
        $ref: '#/definitions/PeerMediaStream'
