get:
  tags:
    - Session Objects
  description: Describe a Connection
  parameters:
    - $ref: '#/parameters/resourceId'
  responses:
    200:
      description: OK
      schema:
        $ref: '#/definitions/ObjectConnection'
